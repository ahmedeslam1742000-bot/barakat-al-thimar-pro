import VoucherWorkspace from './vouchers/VoucherWorkspace';

/** سند إدخال — أدوات التصدير والتصفية (انظر VoucherWorkspace). */
export default function CashIn() {
  return <VoucherWorkspace kind="in" />;
}
